/** Stable object refs for Recharts scatter charts (avoid new identity each render). */
export const SCATTER_MARGIN_DEFAULT = { top: 8, right: 12, left: 0, bottom: 0 }
export const SCATTER_MARGIN_GENRE = { top: 8, right: 12, left: 0, bottom: 32 }
export const SCATTER_CURSOR = { strokeDasharray: '3 3', stroke: '#64748b' }
